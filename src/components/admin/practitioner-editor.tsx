"use client";

import Link from "next/link";
import { useState, useTransition } from "react";
import { useRouter } from "next/navigation";
import { ArrowLeftIcon, ExternalLinkIcon } from "lucide-react";
import { AdminFormField, AdminFormLayout, AdminFormSection } from "@/components/admin/admin-form";
import { AdminPermanentDeleteDialog } from "@/components/admin/record-deletion";
import { PublicLifecycleControls } from "@/components/admin/lifecycle-controls";
import { Button } from "@/components/ui/button";
import { Input } from "@/components/ui/input";
import { Textarea } from "@/components/ui/textarea";
import { formatAdminDate, getPractitionerLifecycle, validatePortraitFile, type TaxonomyRow } from "@/lib/admin/practitioner-cms";
import { archivePractitioner, deletePractitioner, savePractitioner, type AdminPractitionerRecord } from "@/lib/admin/practitioner-actions";

function imageUrl(path: string | null) {
  const base = process.env.NEXT_PUBLIC_SUPABASE_URL;
  return base && path ? `${base}/storage/v1/object/public/profile-images/${path}` : null;
}

type Props = { record: AdminPractitionerRecord | null; terms: TaxonomyRow[]; isNew?: boolean };

export function PractitionerEditor({ record, terms, isNew = false }: Props) {
  const router = useRouter();
  const [status, setStatus] = useState(record ? getPractitionerLifecycle(record) : "draft");
  const [selectedTerms, setSelectedTerms] = useState(() => new Set(record?.terms.map((term) => term.id) ?? []));
  const [file, setFile] = useState<File | null>(null);
  const [approved, setApproved] = useState(false);
  const [dirty, setDirty] = useState(false);
  const [saved, setSaved] = useState(false);
  const [error, setError] = useState<string>();
  const [fieldErrors, setFieldErrors] = useState<Record<string, string>>({});
  const [pending, startTransition] = useTransition();
  const updateSelection = (id: string, checked: boolean) => { setSelectedTerms((current) => { const next = new Set(current); if (checked) next.add(id); else next.delete(id); return next; }); setDirty(true); setSaved(false); };
  const submit = (event: React.FormEvent<HTMLFormElement>) => { event.preventDefault(); const form = new FormData(event.currentTarget); form.set("termIds", JSON.stringify([...selectedTerms])); form.set("status", status); if (file) form.set("portrait", file); if (approved) form.set("imageApproved", "on"); startTransition(async () => { setError(undefined); setFieldErrors({}); setSaved(false); const result = await savePractitioner(form); if (!result.ok) { setFieldErrors(result.fieldErrors ?? {}); setError(result.error); return; } setDirty(false); setSaved(true); if (isNew && result.data?.id) router.replace(`/admin/practitioners/${result.data.id}`); }); };
  const changeStatus = (next: typeof status) => { setStatus(next); setDirty(true); setSaved(false); };
  const archive = (restore = false) => startTransition(async () => { if (!record) return; const result = await archivePractitioner(record.id, restore); if (!result.ok) setError(result.error); else { setStatus(restore ? "draft" : "archived"); setDirty(false); setSaved(true); } });
  const remove = () => startTransition(async () => { if (!record) return; const result = await deletePractitioner(record.id); if (!result.ok) setError(result.error); else router.replace("/admin/practitioners"); });
  const activeTerms = terms.filter((term) => !term.archived_at);
  const grouped = new Map<string, TaxonomyRow[]>();
  for (const term of activeTerms) grouped.set(term.type, [...(grouped.get(term.type) ?? []), term]);
  const currentImage = file ? URL.createObjectURL(file) : imageUrl(record?.image_path ?? null);
  return <div className="mx-auto flex w-full min-w-0 max-w-4xl flex-col gap-6"><div className="flex flex-wrap items-center gap-3"><Button asChild variant="ghost"><Link href="/admin/practitioners"><ArrowLeftIcon />Practitioners</Link></Button></div><AdminFormLayout title={isNew ? "New practitioner" : `Edit ${record?.name ?? "practitioner"}`} description="Keep public profile details specific and current. Saving a published record updates the public site." status={status[0].toUpperCase() + status.slice(1)} statusKind={status} pending={pending} saved={saved} isDirty={dirty} error={error} validationErrors={fieldErrors} onSubmit={submit} onCancel={() => router.replace("/admin/practitioners")} saveLabel={isNew ? "Create practitioner" : "Save changes"}><input type="hidden" name="id" value={record?.id ?? ""} /><AdminFormSection title="Public profile" description="These fields make up the public practitioner record."><div className="grid gap-5"><AdminFormField name="name" label="Name" error={fieldErrors.name}><Input defaultValue={record?.name ?? ""} required onChange={() => { setDirty(true); setSaved(false); }} /></AdminFormField><AdminFormField name="slug" label="Slug" description="Use lowercase words separated by hyphens." error={fieldErrors.slug}><Input defaultValue={record?.slug ?? ""} required onChange={() => { setDirty(true); setSaved(false); }} /></AdminFormField><AdminFormField name="descriptor" label="Descriptor"><Input defaultValue={record?.descriptor ?? ""} onChange={() => setDirty(true)} /></AdminFormField><div className="grid gap-5 sm:grid-cols-2"><AdminFormField name="yearsActive" label="Years active"><Input type="number" min="1" defaultValue={record?.years_active ?? ""} onChange={() => setDirty(true)} /></AdminFormField><div className="grid gap-2"><span className="text-sm font-medium">Delivery</span><label className="flex min-h-11 items-center gap-2 text-sm"><input type="checkbox" name="offersInPerson" defaultChecked={record?.offers_in_person ?? true} onChange={() => setDirty(true)} /> In-person</label><label className="flex min-h-11 items-center gap-2 text-sm"><input type="checkbox" name="offersOnline" defaultChecked={record?.offers_online ?? true} onChange={() => setDirty(true)} /> Online</label></div></div><AdminFormField name="summary" label="Summary" description="Required before publishing." error={fieldErrors.summary}><Textarea defaultValue={record?.summary ?? ""} required={status === "published"} onChange={() => setDirty(true)} /></AdminFormField><AdminFormField name="about" label="About" description="Required before publishing." error={fieldErrors.about}><Textarea defaultValue={record?.about ?? ""} required={status === "published"} onChange={() => setDirty(true)} /></AdminFormField><AdminFormField name="credentials" label="Credentials" description="One item per line or comma separated."><Textarea defaultValue={record?.credentials?.join("\n") ?? ""} onChange={() => setDirty(true)} /></AdminFormField><AdminFormField name="significantTraining" label="Significant training" description="One item per line or comma separated."><Textarea defaultValue={record?.significant_training?.join("\n") ?? ""} onChange={() => setDirty(true)} /></AdminFormField><div className="grid gap-5 sm:grid-cols-2"><AdminFormField name="websiteUrl" label="Website"><Input type="url" defaultValue={record?.website_url ?? ""} onChange={() => setDirty(true)} /></AdminFormField><AdminFormField name="instagramUrl" label="Instagram"><Input type="url" defaultValue={record?.instagram_url ?? ""} onChange={() => setDirty(true)} /></AdminFormField></div></div></AdminFormSection><AdminFormSection title="Portrait" description="Use one approved JPEG, PNG, or WebP portrait up to 5 MB. The image becomes public after upload."><div className="grid gap-4 sm:grid-cols-[10rem_1fr]"><div className="flex min-h-40 items-center justify-center overflow-hidden rounded-md border bg-muted/30">{currentImage ? <img src={currentImage} alt={record?.image_alt ?? "Current portrait"} className="h-full max-h-48 w-full object-cover" /> : <span className="p-4 text-center text-sm text-muted-foreground">No portrait uploaded</span>}</div><div className="grid content-start gap-4"><AdminFormField name="portrait" label="Portrait file" error={fieldErrors.image}><Input type="file" accept="image/jpeg,image/png,image/webp" onChange={(event) => { const next = event.currentTarget.files?.[0] ?? null; setFile(next); setApproved(false); setDirty(true); setFieldErrors((current) => ({ ...current, image: next ? validatePortraitFile(next) ?? "" : "" })); }} /></AdminFormField>{file && <label className="flex min-h-11 items-center gap-2 text-sm"><input type="checkbox" checked={approved} onChange={(event) => { setApproved(event.currentTarget.checked); setDirty(true); }} /> I confirm this portrait is approved for public use.</label>}<AdminFormField name="imageAlt" label="Portrait alt text"><Input defaultValue={record?.image_alt ?? ""} onChange={() => setDirty(true)} /></AdminFormField>{record?.image_path && <a className="inline-flex min-h-11 items-center gap-2 text-sm font-medium underline" href={imageUrl(record.image_path) ?? "#"} target="_blank" rel="noreferrer">Open current portrait <ExternalLinkIcon className="size-4" /></a>}</div></div></AdminFormSection><AdminFormSection title="Taxonomy" description="Select active terms for this practitioner. A published record needs at least one active location."><div className="grid gap-5">{[...grouped.entries()].map(([type, items]) => <fieldset key={type} className="grid gap-2"><legend className="text-sm font-semibold">{type.replaceAll("_", " ").replace(/(^|\s)\S/g, (letter) => letter.toUpperCase())}</legend><div className="grid gap-2 sm:grid-cols-2">{items.map((term) => <label key={term.id} className="flex min-h-11 items-center gap-2 rounded-md border px-3 text-sm"><input type="checkbox" checked={selectedTerms.has(term.id)} onChange={(event) => updateSelection(term.id, event.currentTarget.checked)} />{term.name}</label>)}</div></fieldset>)}{fieldErrors.location && <p className="text-sm text-destructive" role="alert">{fieldErrors.location}</p>}</div></AdminFormSection><div className="grid gap-4"><section className="rounded-md border bg-card p-4"><PublicLifecycleControls value={status} onChange={changeStatus} onArchive={() => archive(false)} disabled={pending} recordName={record?.name ?? "this practitioner"} /></section>{record && <section className="rounded-md border bg-card p-4"><div className="flex flex-wrap items-center justify-between gap-3"><div><h2 className="font-semibold">Record actions</h2><p className="mt-1 text-sm text-muted-foreground">Created {formatAdminDate(record.created_at)}. Archive before permanent deletion.</p></div>{status === "archived" && <div className="flex flex-wrap gap-2"><Button type="button" variant="outline" onClick={() => archive(true)} disabled={pending}>Restore to draft</Button><AdminPermanentDeleteDialog recordName={record.name} onDelete={remove} disabled={pending} /></div>}{record.image_path && status === "archived" && <p className="w-full text-sm text-muted-foreground">Remove the stored portrait through Storage before permanent deletion.</p>}</div></section>}</div></AdminFormLayout></div>;
}
